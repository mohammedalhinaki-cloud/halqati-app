{
  "name": "quran-halaqa-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "بطاقة متابعة الحفظ والمراجعة — Quran Halaqa Tracker (Next.js 14 + Tailwind, static, localStorage)",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "next dev -H 0.0.0.0 -p 3000",
    "build": "next build",
    "start": "next start -H 0.0.0.0",
    "gen": "node scripts/gen-quran.mjs",
    "test": "node scripts/test-plan.mjs",
    "pretest": "npm run gen",
    "test:ui": "npx esbuild scripts/render-test.mjs --bundle --platform=node --format=esm --outfile=.tmp-render-test.mjs --loader:.jsx=jsx --jsx=automatic --packages=external --log-level=error && node .tmp-render-test.mjs && rm -f .tmp-render-test.mjs",
    "test:click": "npx esbuild scripts/click-entry.mjs --bundle --platform=node --format=esm --outfile=.tmp-click-entry.mjs --loader:.jsx=jsx --jsx=automatic --packages=external --log-level=error && node scripts/click-none-test.mjs && rm -f .tmp-click-entry.mjs",
    "test:all": "npm run test --silent && npm run test:ui --silent && npm run test:click --silent && npm run test:dom --silent",
    "test:dom": "node scripts/jsdom-smoke.mjs"
  },
  "dependencies": {
    "moment": "^2.30.1",
    "moment-hijri": "^3.0.0",
    "next": "^14.2.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "esbuild": "^0.28.2",
    "jsdom": "^29.1.1",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.14"
  },
  "type": "module"
}